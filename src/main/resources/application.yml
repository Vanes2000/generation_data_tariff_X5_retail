spring:
  datasource:
    url: "jdbc:mysql://10.63.2.161:30000/test_crm?useSSL=false&serverTimezone=UTC"
    username: root
    password: NXX1BjPbF9
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      maximum-pool-size: 3
      minimum-idle: 1
      connection-timeout: 5000
      idle-timeout: 300000
      leak-detection-threshold: 10000

logging:
  level:
    org.springframework.jdbc: DEBUG
    org.springframework.transaction: DEBUG
    org.example.connectionDataBase: DEBUG